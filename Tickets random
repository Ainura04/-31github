#include <iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
    
   {
   if (n < 60)
   cout<<"Process A will run!"<<endl;
   if (n > 60)
   cout<<"Process B will run!";
   }
    return 0;
}
